name = "futbolcanli"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Account - küçük-çimen-c8d8
# Account ID: 03c658bb7282c34d0ee4b67a0357b03e
# NOT: Express Node server Worker'da calismaz — bu proje Pages (static) olarak deploy edilir.
# Canli API VPS/Tunnel uzerinde ayri calisir, Pages offline bundle (public/data/latest.json) ile fallback yapar.

[assets]
directory = "./dist"
not_found_handling = "single-page-application"

[vars]
NODE_ENV = "production"

# R2 Binding - Dashboard > R2 > Enable R2 sonra ac
# [[r2_buckets]]
# binding = "FUTBOLCANLI_DATA"
# bucket_name = "futbolcanli-data"

[observability]
enabled = true

[build]
command = "npm run build"
